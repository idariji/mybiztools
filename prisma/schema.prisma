// Prisma Schema for MyBizTools Admin System
// Run migrations after updating:
// npx prisma migrate dev --name <migration-name>
// npx prisma generate

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

// ============================================================================
// USER MODELS
// ============================================================================

model User {
  id                    String    @id @default(cuid())
  email                 String    @unique
  name                  String?
  phone                 String?
  avatar_url            String?
  
  // Subscription
  subscription          Subscription?
  current_plan          String    @default("free")
  subscription_status   String    @default("active")
  
  // Metadata
  created_at            DateTime  @default(now())
  updated_at            DateTime  @updatedAt
  last_login_at         DateTime?
  ip_address            String?
  country               String?
  device_info           String?

  // Relations
  payments              Payment[]
  audit_logs            AdminAuditLog[]
  overrides             PromotionalOverride[]
  usage_tracking        UsageTracking?
  abuse_reports         AbuseReport[]
  
  @@index([email])
  @@index([subscription_status])
  @@index([current_plan])
}

// ============================================================================
// SUBSCRIPTION MODELS
// ============================================================================

model Subscription {
  id                        String    @id @default(cuid())
  user_id                   String    @unique
  user                      User      @relation(fields: [user_id], references: [id], onDelete: Cascade)
  
  plan_name                 String    // "free", "pro", "enterprise"
  status                    String    @default("active")
  
  // Dates
  current_period_start      DateTime
  current_period_end        DateTime
  trial_end_date            DateTime?
  expires_at                DateTime?
  cancelled_at              DateTime?
  cancelled_reason          String?
  
  // Billing
  mrr_value                 BigInt    @default(0) // Monthly Recurring Revenue in Naira
  annual_value              BigInt    @default(0)
  is_paused                 Boolean   @default(false)
  auto_renew                Boolean   @default(true)
  
  // Payment method
  payment_method_id         String?
  last_payment_id           String?
  
  created_at                DateTime  @default(now())
  updated_at                DateTime  @updatedAt

  // Relations
  manual_changes            ManualSubscriptionChange[]
  
  @@index([plan_name])
  @@index([status])
  @@index([current_period_end])
}

// ============================================================================
// PAYMENT MODELS
// ============================================================================

model Payment {
  id                    String    @id @default(cuid())
  user_id               String
  user                  User      @relation(fields: [user_id], references: [id], onDelete: Cascade)
  subscription_id       String?
  
  amount                BigInt    // Amount in Naira
  currency              String    @default("NGN")
  status                String    // "succeeded", "failed", "pending", "refunded"
  
  // Payment Gateway
  stripe_payment_id     String?   @unique
  stripe_invoice_id     String?
  
  // Billing period
  billing_period_start  DateTime?
  billing_period_end    DateTime?
  
  // Failure tracking
  failure_reason        String?
  retry_count           Int       @default(0)
  next_retry_at         DateTime?
  max_retries_reached   Boolean   @default(false)
  
  // Refund
  refunded_amount       BigInt    @default(0)
  refund_reason         String?
  refunded_at           DateTime?
  
  created_at            DateTime  @default(now())
  updated_at            DateTime  @updatedAt

  // Relations
  refund_logs           RefundLog[]
  
  @@index([user_id])
  @@index([status])
  @@index([created_at])
  @@index([stripe_payment_id])
}

model RefundLog {
  id                    String    @id @default(cuid())
  payment_id            String
  payment               Payment   @relation(fields: [payment_id], references: [id], onDelete: Cascade)
  
  amount                BigInt    // Refund amount
  reason                String
  admin_id              String
  admin_name            String
  
  processed_at          DateTime  @default(now())
  
  @@index([payment_id])
}

// ============================================================================
// SUBSCRIPTION MANAGEMENT MODELS
// ============================================================================

model ManualSubscriptionChange {
  id                    String    @id @default(cuid())
  subscription_id       String
  subscription          Subscription @relation(fields: [subscription_id], references: [id], onDelete: Cascade)
  user_id               String
  
  admin_id              String
  admin_name            String
  admin_role            String
  
  action                String    // "upgrade", "downgrade", "suspend", "reactivate", "extend"
  from_plan             String?
  to_plan               String?
  
  effective_date        DateTime
  reason                String
  notes                 String?
  
  status                String    @default("pending")
  approved_at           DateTime?
  completed_at          DateTime?
  
  created_at            DateTime  @default(now())
  updated_at            DateTime  @updatedAt
  
  @@index([user_id])
  @@index([admin_id])
  @@index([action])
}

model PromotionalOverride {
  id                    String    @id @default(cuid())
  user_id               String
  user                  User      @relation(fields: [user_id], references: [id], onDelete: Cascade)
  
  override_type         String    // "free_promo", "discount", "plan_upgrade", "feature_unlock", "comp_account"
  plan_override         String?   // Plan to override to
  discount_percentage   Float?
  discount_amount       BigInt?   // In Naira
  specific_features     String[]  // JSON array of feature IDs
  
  start_date            DateTime
  end_date              DateTime
  is_active             Boolean   @default(true)
  
  reason                String
  applied_by            String
  
  created_at            DateTime  @default(now())
  updated_at            DateTime  @updatedAt
  
  @@index([user_id])
  @@index([is_active])
  @@index([end_date])
}

// ============================================================================
// USAGE TRACKING MODELS
// ============================================================================

model UsageTracking {
  id                    String    @id @default(cuid())
  user_id               String    @unique
  user                  User      @relation(fields: [user_id], references: [id], onDelete: Cascade)
  
  plan                  String
  
  // Feature usage (JSON format)
  feature_usage         Json      // { [feature]: { used, limit, resetDate } }
  
  last_reset_date       DateTime  @default(now())
  next_reset_date       DateTime
  
  // Status
  is_over_quota         Boolean   @default(false)
  warnings_count        Int       @default(0)
  
  created_at            DateTime  @default(now())
  updated_at            DateTime  @updatedAt
  
  @@index([plan])
  @@index([is_over_quota])
}

// ============================================================================
// ABUSE & SECURITY MODELS
// ============================================================================

model AbuseReport {
  id                    String    @id @default(cuid())
  user_id               String
  user                  User      @relation(fields: [user_id], references: [id], onDelete: Cascade)
  
  report_type           String    // "quota_abuse", "payment_fraud", "api_abuse", "free_account_spam", "ai_abuse", "deletion_loops"
  severity              String    // "low", "medium", "high", "critical"
  
  description           String
  evidence              Json?     // JSON with details
  
  status                String    @default("pending")
  flagged_at            DateTime  @default(now())
  reviewed_at           DateTime?
  reviewed_by           String?
  
  action_taken          String?   // "suspended", "warned", "dismissed", "overridden"
  action_reason         String?
  
  created_at            DateTime  @default(now())
  updated_at            DateTime  @updatedAt
  
  @@index([user_id])
  @@index([severity])
  @@index([status])
}

// ============================================================================
// ADMIN MODELS
// ============================================================================

model AdminAuditLog {
  id                    String    @id @default(cuid())
  
  admin_id              String
  admin_name            String
  admin_role            String
  
  action                String    // "create", "read", "update", "delete"
  resource              String    // "user", "subscription", "payment", "override", etc
  resource_id           String
  
  changes               Json?     // What was changed
  reason                String?
  
  ip_address            String?
  user_agent            String?
  
  created_at            DateTime  @default(now())
  
  @@index([admin_id])
  @@index([action])
  @@index([resource])
  @@index([created_at])
}

model AdminRole {
  id                    String    @id @default(cuid())
  name                  String    @unique
  level                 Int       // 1-4 for hierarchy
  
  permissions           String[]  // Action permissions
  
  created_at            DateTime  @default(now())
  updated_at            DateTime  @updatedAt
}

// ============================================================================
// INDEXING STRATEGY
// ============================================================================
// 
// Key indexes for performance:
// - User.email (unique), .subscription_status, .current_plan
// - Subscription.plan_name, .status, .current_period_end
// - Payment.user_id, .status, .created_at, .stripe_payment_id
// - ManualSubscriptionChange.user_id, .admin_id, .action
// - AbuseReport.user_id, .severity, .status
// - AdminAuditLog.admin_id, .action, .resource, .created_at
//
// Compound indexes for common queries:
// - (user_id, status) on Payment
// - (user_id, is_active) on PromotionalOverride
// - (severity, status) on AbuseReport
